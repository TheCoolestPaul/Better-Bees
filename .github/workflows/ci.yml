name: CI
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
  workflow_dispatch:
    inputs:
      profile:
        description: Validation coverage
        type: choice
        default: routine
        options: [routine, full]
      world_upgrade:
        description: Force sequential saved-world upgrade tests (also selected automatically for persistence changes)
        type: boolean
        default: false
permissions: { contents: read }
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validation:
    uses: ./.github/workflows/validate.yml
    with:
      ref: ${{ github.sha }}
      profile: ${{ github.event_name == 'workflow_dispatch' && inputs.profile || 'routine' }}
      base_ref: ${{ github.event.pull_request.base.sha || github.event.before || format('{0}^', github.sha) }}
      world_upgrade: ${{ github.event_name == 'workflow_dispatch' && inputs.world_upgrade || false }}
